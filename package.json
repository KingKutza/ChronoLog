{
  "name": "chronolog-pocket-instrument",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node tools/serve.js",
    "test": "node --test",
    "check": "node --check src/*.js && node --check tools/*.js"
  }
}
